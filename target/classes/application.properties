# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
spring.datasource.username=rm559123
spring.datasource.password=fiap
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate Configuration
# create = apaga e recria a tabela toda vez
# update = cria se não existir, atualiza se existir (mantém dados)
# validate = apenas valida o schema, não altera nada
# none = não faz nada
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
server.port=8081
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.mapper.accept_case_insensitive_enums=true
